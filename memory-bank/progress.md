# Progress
We are in the data extraction phase, from various sources.

+ The first step is to download data from Garmin
  + Initial scaffolding added: `.env`, `.gitignore` ignores for secrets, `src/garmin_client.py`, and `scripts/garmin_fetch_example.py`.
+ Evaluate what can be extracted, what is available hierarchically and what level of detail is reasonable to store.
+ Next, Withings data
+ Then, org-mode notes parsing
  + Journal
  + Resistance
+ Decide on database+schema
- First dashboard prototype
- Basic command to update data
- MCP server
- Gandi + caddy SSL
- Test from LLM
- Discuss and calculate useful aggregates
- Download my entire history from Garmin and Withings
- Parse all journals